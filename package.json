{
  "name": "recetas-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Blog de recetas: frontend React (Vite) + backend Node/Express",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "start": "npm run start -w backend",
    "build": "npm run build -w frontend",
    "lint": "echo 'Sin linter configurado'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "sass": "^1.93.2"
  }
}
